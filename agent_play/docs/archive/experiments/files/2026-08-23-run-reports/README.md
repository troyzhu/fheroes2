# Vendored run reports, 2026-08-23-run-reports

Provenance, not a reading path: every number these files carry is already quoted in a dated log under `../..`, and this index exists so a reader can find the one artifact behind a claim without opening a hundred of them. Generated by `agent_play/experiments/index_run_reports.py`; the documentation gate fails when a file here matches no family below.

| Files | Count | Produced by | What it answers |
|---|---|---|---|
| `play_fixed_*` | 3 | `search_agent_battery.py under the exclusion arm` | The searched agent with un-appliable candidates excluded rather than substituted, three seeds
      on the mirrors with honest dice; the arm that measured -0.139 win rate and was not adopted |
| `play_old_*` | 3 | `search_agent_battery.py under the substituting default` | The same three seeds with the standing behaviour, a candidate the replay cannot offer scored
      by the skip the engine substitutes; the control the exclusion arm lost to |
| `replay_divergence.json` | 1 | `replay_divergence.py --report` | How far the side environment's replay drifts from the live battle under the mandated offset:
      21.6 percent of candidates un-appliable, 36 percent by depth twelve, 0 on shared dice |

