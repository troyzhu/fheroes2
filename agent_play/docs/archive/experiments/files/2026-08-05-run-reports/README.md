# Vendored run reports, 2026-08-05-run-reports

Provenance, not a reading path: every number these files carry is already quoted in a dated log under `../..`, and this index exists so a reader can find the one artifact behind a claim without opening a hundred of them. Generated by `agent_play/experiments/index_run_reports.py`; the documentation gate fails when a file here matches no family below.

| Files | Count | Produced by | What it answers |
|---|---|---|---|
| `dagger*` | 11 | `dagger_iteration.py` | A DAgger round: student-played states relabeled by the planner probe, and what the retrained clone scored |
| `thunk_*` | 11 | `thunk_validation.py` | The Thunk opening fight as a standing ladder, per checkpoint and per rung |
| `critic*` | 7 | `critic_pre_fitting.py, critic_calibration.py, critic_on_pool.py` | A critic fit and where it actually works: explained variance and bias on teacher against student states |
| `gen*` | 7 | `search_teacher.py generations` | A search-taught generation's collection or evaluation |
| `pool*` | 5 | `the scenario generator` | A standing matchup pool that later runs evaluate against |
| `*_manifest*` | 4 | `the recording scripts` | A dataset's matchup list and settings; corpora re-record from these rather than being vendored whole |
| `arms*` | 3 | `advantage_and_trust_region.py` | Advantage estimators and trust regions compared, on one matchup or on a pool |
| `bc*` | 3 | `train_bc.py` | A behavior-cloning run's history and its best-agreement checkpoint |
| `census_*` | 3 | `the corpus census` | What a recorded corpus contains, by matchup and by decision |
| `floor*` | 3 | `advantage_floor.py` | Whether flooring the advantage-normalization divisor stops a converged run destroying itself |
| `margin*` | 2 | `the reward experiments` | A margin-form reward measured against its alternative |
| `ppo_*` | 2 | `ppo_from_strongest.py and the PPO harnesses` | A reinforcement arm's own report: what it gained on the pool and what it did to held-out play |
| `telem_*` | 2 | `advantage_floor.py telemetry` | Per-iteration telemetry behind a floor verdict |
| `thr*` | 2 | `advantage_and_trust_region.py --threshold` | The divergence trust region at a given threshold |
| `ablation*` | 1 | `encoding_ablation.py` | Which encoding features earn their place, on an episode split and a count-extrapolation split |
| `battlefield_spread*` | 1 | `battlefield_spread.py` | How much the obstacle layout alone moves a matchup's win rate |
| `capacity*` | 1 | `capacity.py` | Whether network size binds, for cloning or for reinforcement |
| `credit_assignment*` | 1 | `credit_assignment.py` | How often trajectory-level credit mis-signs a decision, judged by search rollout values |
| `difficulty_reward*` | 1 | `difficulty_reward.py` | Whether difficulty-weighting the terminal reward changes what pool training learns |
| `diverse_pick*` | 1 | `record_diverse.py` | Which matchups the diverse recording kept |
| `mirror_symmetry*` | 1 | `symmetry_gap.py predecessor` | An early chair-swap measurement |
| `planner_query*` | 1 | `planner_query.py` | Whether querying the built-in planner perturbs the battle, paired terminal digests |
| `*value*` | 1 | `the value-estimation lab` | A fitted value estimator and what it explained, kept because the thread is an educational record |
| `replay_*` | 1 | `capture_replay.py, render_replay.py` | A recorded episode kept so it can be rendered again |
| `search_*` | 1 | `search_probe.py, search_teacher.py, search_agent_battery.py` | What root search measures or plays: per-candidate values, agreement, or suite results |
| `side_swap*` | 1 | `the chair experiments` | A chair-swap check on recorded episodes |
| `smoke*` | 1 | `any harness` | A smoke run kept because a later claim cites it |
| `teacher_winrates*` | 1 | `the teacher census` | The built-in planner's own rates on the pool |
| `validation_battery*` | 1 | `validation_battery.py` | An early battery report predating the per-arm naming |

