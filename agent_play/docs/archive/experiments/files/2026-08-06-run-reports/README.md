# Vendored run reports, 2026-08-06-run-reports

Provenance, not a reading path: every number these files carry is already quoted in a dated log under `../..`, and this index exists so a reader can find the one artifact behind a claim without opening a hundred of them. Generated by `agent_play/experiments/index_run_reports.py`; the documentation gate fails when a file here matches no family below.

| Files | Count | Produced by | What it answers |
|---|---|---|---|
| `*_manifest*` | 24 | `the recording scripts` | A dataset's matchup list and settings; corpora re-record from these rather than being vendored whole |
| `battery_*` | 15 | `validation_battery.py` | A report card over the suites for a named arm: rates per suite, per-rung ladders, and the quality columns |
| `*distill*` | 5 | `soft_distill.py, awr_distill.py` | A paired distillation run: the arm, its twin, target entropy and per-epoch history |
| `dagger*` | 5 | `dagger_iteration.py` | A DAgger round: student-played states relabeled by the planner probe, and what the retrained clone scored |
| `planes_ablation*` | 3 | `planes_ablation.py` | The spatial-observation ablation with its width-matched capacity control |
| `q_one_step*` | 2 | `a vendored one-off script and its log` | The behavior-Q one-step rerank measurement, kept with the script that produced it |
| `*value*` | 2 | `the value-estimation lab` | A fitted value estimator and what it explained, kept because the thread is an educational record |
| `ability_ablation*` | 1 | `ability_ablation.py` | Whether explicit per-creature ability features earn their place |
| `builtin_ai_baseline*` | 1 | `builtin_ai_baseline.py` | The engine's own AI on every validation suite, the bar the policies must clear; the `_v2` form adds the quality columns |
| `evasion_stalemate*` | 1 | `evasion_stalemate.py` | The stalling exploit demonstrated and priced |
| `replay_*` | 1 | `capture_replay.py, render_replay.py` | A recorded episode kept so it can be rendered again |
| `search_*` | 1 | `search_probe.py, search_teacher.py, search_agent_battery.py` | What root search measures or plays: per-candidate values, agreement, or suite results |
| `symmetry_*` | 1 | `symmetry_gap.py` | The same army from either chair against the engine's own asymmetry, which is the neutral point |
| `tabula_rasa_pilot*` | 1 | `tabula_rasa_pilot.py` | Whether search bootstraps a policy from random initialization |

