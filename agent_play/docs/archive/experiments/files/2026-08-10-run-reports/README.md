# Vendored run reports, 2026-08-10-run-reports

Provenance, not a reading path: every number these files carry is already quoted in a dated log under `../..`, and this index exists so a reader can find the one artifact behind a claim without opening a hundred of them. Generated by `agent_play/experiments/index_run_reports.py`; the documentation gate fails when a file here matches no family below.

| Files | Count | Produced by | What it answers |
|---|---|---|---|
| `spread_s*` | 5 | `search_agent_battery.py --seed, one suite repeated` | How far one battery number moves between runs of an identical configuration, which nothing had
      measured because the harness seeded nothing; the error bar every single-run search figure lacked |
| `honest_ladder_*` | 4 | `search_agent_battery.py at rising budgets` | The simulation ladder redone with the side environment on the live battlefield and independent
      dice; the published ladder had neither and every rung of it was an upper bound |
| `deploy_search_*` | 3 | `search_agent_battery.py --deployment` | The action rule crossed with search for the first time, with the full column block including
      both entropy forms and the search's own visit entropy; shows greedy starving the search |
| `final_sweep_*` | 3 | `search_agent_battery.py, every correction applied` | The goal-45 scoreboard with the survival-graded objective, the live battlefield, independent dice
      and a seed, three of them, which is the first version of this table with a real error bar |
| `fullmetrics_s*` | 3 | `search_agent_battery.py with the full column block` | The goal-45 scoreboard with every column the harness can now report, three seeds and a paired
      unsearched arm; the version that replaces the rates-only sweep beside it |
| `search_*` | 3 | `search_probe.py, search_teacher.py, search_agent_battery.py` | What root search measures or plays: per-candidate values, agreement, or suite results |
| `search_strength*` | 2 | `search_strength.py` | Simulation budget against PUCT exploration weight against coverage forcing, with the candidates
      each cell actually visited, which is what shows forcing spending a budget on breadth |
| `teacher_leakage*` | 2 | `teacher_leakage.py` | Whether knowing the dice changes what the search teacher labels, asked at identical states, with a
      second same-information teacher as the winner's-curse control that the first run lacked |
| `battery_margin*` | 1 | `validation_battery.py` | The confidence-filter distillation arms, with and without the single-playout tie-break overrules,
      three paired seeds each; the arm that measured negative and was not adopted |
| `battery_noforce*` | 1 | `validation_battery.py` | Distillation from a corpus collected without coverage forcing against the forced corpus, three
      paired seeds at matched soft mass; confident labels that net nothing and trade axes instead |
| `battery_*` | 1 | `validation_battery.py` | A report card over the suites for a named arm: rates per suite, per-rung ladders, and the quality columns |
| `honest_sweep*` | 1 | `search_agent_battery.py --search-combat-offset` | The searching agent on every suite with its side environment on the live battlefield but its dice
      independent, beside a paired unsearched control; the honest reading of the goal-45 scoreboard |
| `label_value*` | 1 | `label_value.py` | Whether a search overrule actually beats the policy's own choice, by replaying the battle from
      each branch on independently drawn dice; the causal test that label-noise measurements cannot settle |
| `push45_*` | 1 | `search_agent_battery.py with the live dice` | The same sweep with the side environment sharing the live world seed, so search saw the rolls the
      battle was about to make; a ceiling rather than a score, kept because the retraction cites it |
| `terrain_probe*` | 1 | `the mirror terrain probe` | Identical armies on six battlefields, isolating how much of a matchup's win rate is the obstacle
      layout rather than the armies |

